
dependencies {
    compile project (":transport")
}

jar {
    manifest {
        attributes 'Main-Class': 'com.temas.protocols.benchmark.sctp.SctpServerKt'
    }
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
}